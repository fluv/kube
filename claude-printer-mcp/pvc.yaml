apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: printer-mcp-data
  namespace: claude-printer-mcp
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
