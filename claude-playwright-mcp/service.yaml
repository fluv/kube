apiVersion: v1
kind: Service
metadata:
  name: playwright-mcp
  namespace: claude-playwright-mcp
spec:
  selector:
    app: playwright-mcp
  ports:
    - name: http
      port: 8931
      targetPort: http
