apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: notebook-data
  namespace: claude-notebook
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
