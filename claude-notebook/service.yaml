apiVersion: v1
kind: Service
metadata:
  name: notebook
  namespace: claude-notebook
  labels:
    app: notebook
spec:
  selector:
    app: notebook
  ports:
    - name: http
      port: 8080
      targetPort: http
